import * as React from "react";
import { render, fireEvent, getByText, screen } from "@testing-library/react";
import { expect, test } from "@jest/globals";
import { RouterProvider, createMemoryRouter,Route,BrowserRouter,Routes } from "react-router-dom";


describe("testing",()=> { 
    it('doing',()=> { 
        expect(4+4).toEqual(8)
    })
})